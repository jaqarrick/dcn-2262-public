# Data Comms. & Networks - Lab 3
Each server is a python app. Fibonacci & User servers are simple flask apps (in ./FS and ./US directories). The Authoritative server is a UDP server connected to a simple sqlite database, which is created when the project runs.

Each server can be run with 

```
make install
make run
```

